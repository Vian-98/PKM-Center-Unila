# PKM-Center-Unila

Aplikasi awal PKM Center Universitas Lampung, terdiri dari frontend React, REST API Golang/Gin, dan PostgreSQL yang dijalankan dengan Docker Compose.

## Menjalankan aplikasi

```bash
docker compose up --build
```

- Frontend: `http://localhost:5173`
- API: `http://localhost:8081`
